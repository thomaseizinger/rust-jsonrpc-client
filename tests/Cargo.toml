[package]
name = "tests"
version = "1.0.0"
authors = [ "Thomas Eizinger <thomas@eizinger.io>" ]
edition = "2018"
license = "MIT OR Apache-2.0"
publish = false

[dependencies]
jsonrpc_client = { path = "../lib" }
